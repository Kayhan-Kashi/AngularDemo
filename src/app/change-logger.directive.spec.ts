import { ChangeLoggerDirective } from './change-logger.directive';

describe('ChangeLoggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeLoggerDirective();
    expect(directive).toBeTruthy();
  });
});
